import { MatchValueDirective } from './match-value.directive';

describe('MatchValueDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchValueDirective();
    expect(directive).toBeTruthy();
  });
});
